The *CronWithRetry* sample demonstrates how to run a scheduled task with ``exponential_retry`` options. Once the
workflow is complete, ``continue_as_new`` is used to re-run the workflow at the next scheduled time.


